%%  x^2 + 10x = 39: x = 3
\documentclass[tikz]{standalone}
\usepackage{standalone}
\usepackage{amsmath,amsfonts,amssymb,mathrsfs,tikz,fancyhdr}
\usepackage{fontspec}

\begin{document}
\providecommand{\jadhr}{3}

%% X axis coordinates
\providecommand{\Bx}{5}
\providecommand{\Ax}{8}

%% Y axis
\providecommand{\By}{5}
\providecommand{\Ay}{8}

%% A ا B ٮ C ج D د E ه F ز G ح H ط J ك K ل L م M ن
\providecommand{\A}  {A}%% {ا}
\providecommand{\Ba} {B}  %% {ٮ}
\providecommand{\Jīm}{J} %% {ج}
\providecommand{\Dal}{D} %% {د}
\providecommand{\Ha} {H}  %% {ه}
\providecommand{\Z}  {Z}   %% {ز}
\providecommand{\HH} {Ḥ}  %% {ح}
\providecommand{\TT} {Ṭ}  %% {ط}
\providecommand{\K}  {K}   %% {ك}
\providecommand{\Lam}{L} %% {ل}
\providecommand{\M}  {M}   %% {م}
\providecommand{\N}  {N}   %% {ن}

\begin{tikzpicture}[scale=0.8]
  %% Baseline X coordinates (RTL): د ج
  \coordinate [label={below right:{\Ha}}] (H) at (\Ax, 0);

  %% Midline 1 X coordinates (RTL): ٮ
  \coordinate [label={above right:{\Ba}}] (B) at (\Bx, \By);

  %% Top
  \coordinate [label={above left:{\Z}}] (Z) at (0, \Ay);
  \coordinate [label={below left:{\A}}] (A) at (\Ax, \Ay);

  %% surfaces
  %% māl
  \draw [thick,red] (\Bx,\By) -- (\Ax,\By)
  -- (\Ax,\Ay) -- (\Bx,\Ay) -- cycle;
  \coordinate [label={center:{$x^2$}}] ()
  at (\Bx + \jadhr/2, \By + \jadhr/2);

  %% lower left square
  \draw [thick,red] (0,0) -- (\Bx, 0)
  -- (\Bx,\By) -- (0,\By) -- cycle;
  \coordinate [label={center:{$25$}}] ()
  at (0 + \Bx/2, \By/2);

  %% upper left rect (Jīm)
  \draw [thick] (0,\By) -- (0,\Ay) -- (\Bx,\Ay);
  \coordinate [label={right:{$x$}}] ()
  at (0, \By + \jadhr/2);
  \coordinate [label={below:{$\alpha=\tfrac{10}{2}$}}] ()
  at (\Bx / 2, \Ay);
  \coordinate [label={center:{\Jīm}}] ()
  at (0 + \Bx/2, \By + \jadhr/2);

  %% lower right rect (Dal)
  \draw [thick] (\Bx,0) -- (\Ax,0) -- (\Ax,\By);
  \coordinate [label={left:{$\alpha$}}] ()
  at (\Ax, \By/2);
  \coordinate [label={above:{$x$}}] ()
  at (\Bx + \jadhr/2, 0);
  \coordinate [label={center:{\Dal}}] ()
  at (\Bx + \jadhr/2, \By/2);

  \coordinate [label={right:{$x^2+J+D=39$}}] ()
  at (\Ax + 0.2, \Ay);

  % fig label
  \coordinate [label={below:{$x^2 + 10x = 39\ \therefore\ x=3$}}] ()
  at (4, 0);
\end{tikzpicture}

\end{document}